# Dependencias exclusivas da venue Nado (DEX_ID=nado).
# Separadas do requirements.txt porque exigem toolchain de build nativo
# (web3 + extensoes C): quem opera Hyperliquid/CEX nao precisa delas.
-r requirements.txt
nado-protocol==0.3.5

# `eth-keyfile`, que vem com o SDK da Nado, ainda importa `pkg_resources`, e
# `setuptools>=81` removeu essa API. A restricao vive aqui e nao no
# `requirements.txt` porque so quem usa a Nado tem `eth-keyfile`: no base ela
# limitava o setuptools de toda instalacao por causa de uma dependencia que a
# maioria nao instala.
setuptools<81
